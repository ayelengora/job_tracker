services:
  - type: web
    name: job-tracker
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /up
    envVars:
      # Postgres vive en Neon (free, no expira), no en Render.
      # Pegá acá el connection string que te da Neon (con ?sslmode=require al final).
      - key: DATABASE_URL
        sync: false
      - key: RAILS_MASTER_KEY
        sync: false
